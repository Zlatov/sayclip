# Этапы разработки sayclip

> «Скажи — попадёт в буфер обмена.»

Каждый этап — маленький и самостоятельно тестируемый. Проверка всегда
проходит в роли конечного пользователя: человека, который ничего не знает
о внутренностях, просто ставит пакет и пробует им пользоваться.

Отличие рабочего процесса от debash: **git-коммиты, push и npm-публикацию
делает пользователь сам**, вручную, на каждом этапе. Ассистент готовит код
и описывает, что и как проверить, но не выполняет `git commit`/`git push`/
`npm publish` за пользователя.

---

## Хотелки (собрано до разбивки на этапы)

* push-to-talk — зажал хоткей, говоришь, отпустил — не toggle;
* запуск как демон из консоли: `sayclip start` / `sayclip stop`, либо
  просто `sayclip` — чтобы видеть лог в foreground;
* возможность добавить демон в автостарт системы;
* `sayclip reconfig` (или `--reconfig`) — перенастройка без переустановки;
* переключение локальной модели whisper (список моделей — например, через
  `ls` по заданной директории);
* переключение языка распознавания (в debash язык зашит намертво — здесь
  должен быть настраиваемым);
* возможность задать базовый промт для Whisper (initial prompt — влияет на
  словарь/стиль распознавания);
* результат транскрибации сразу попадает в буфер обмена;
* уведомление пользователя о результате/ошибке/отмене — на Linux
  `notify-send`, на Windows — toast/трей (конкретный механизм решить на
  соответствующем этапе);
* Linux — основная платформа, Windows — обязательна, но вторична по
  приоритету;
* транскрибация — только локальный whisper по пути к бинарнику/модели,
  без поднятия HTTP API (как в debash).

---

## Открытые решения (зафиксировано)

* **Имя пакета:** `sayclip` — свободно на npm (проверено 2026-08-20),
  займём место при первой публикации.
* **Push-to-talk:** честный hold-режим (`keydown` → запись, `keyup` →
  стоп) через `uiohook-napi`, не bracket-toggle.
* **Репозиторий:** публичный (как debash).
* **Лицензия:** MIT (как в debash).
* **Git/npm:** репозиторий, коммиты, push и публикацию выполняет
  пользователь сам. Порядок релизного цикла: edit code → `git commit`
  (чистое дерево обязательно) → `npm version patch` (сам коммитит и
  тегирует версию, но не пушит) → `git push --follow-tags` → `npm
  publish`.

---

## Этап 0 — Скелет CLI

Node.js проект, `package.json`, точка входа `bin/sayclip`, при запуске без
аргументов печатает заголовок/версию и ничего не падает.

**Тест:** `node bin/sayclip.js` → видно шапку, процесс не падает.

---

## Этап 1 — Инфраструктура публикации

`package.json`: `name: sayclip`, `bin`, `license`, `files`, минимальный
README для npm-страницы. Git-репозиторий, первый коммит, публикация первой
версии — всё вручную, пользователем.

**Тест:** после `npm install -g sayclip` — команда `sayclip` доступна
глобально, печатает ту же шапку, что и на этапе 0.

---

## Этап 2 — Локальная транскрибация по готовому файлу

Конфиг с путём к whisper-бинарнику и модели (по аналогии с debash — без
HTTP API). Команда вида `sayclip transcribe <файл.wav>` — прогоняет уже
готовый аудиофайл через whisper, печатает текст в терминал. Хоткея и записи
микрофона ещё нет — проверяем только сам pipeline транскрибации.

**Тест:** заранее записанный `.wav` с русской/английской речью →
`sayclip transcribe test.wav` → в терминале корректный текст.

---

## Этап 3 — Запись микрофона + push-to-talk хоткей (Linux)

`uiohook-napi` слушает глобальный хоткей, различая `keydown`/`keyup` (см.
открытое решение выше). Пока клавиша зажата — идёт запись с микрофона во
временный `.wav`, по отпусканию — файл уходит в whisper (как на этапе 2).

**Тест:** зажал хоткей, сказал фразу, отпустил → в терминале лога виден
распознанный текст.

---

## Этап 4 — Буфер обмена и уведомления (Linux)

Результат транскрибации копируется в буфер обмена (`xclip`/`xsel`/
`wl-copy` в зависимости от сессии). Уведомление об успехе/ошибке через
`notify-send`.

**Тест:** зажал хоткей → сказал фразу → отпустил → всплыло уведомление,
`Ctrl+V` в любом текстовом поле вставляет ровно то, что было сказано.

---

## Этап 5 — Демон-обвязка

`sayclip start` (фон) / `sayclip stop` / просто `sayclip` (foreground с
логом). Инструкция/скрипт для добавления в автостарт системы (systemd
user unit для Linux).

**Тест:** `sayclip start`, перезапуск сессии/системы с автостартом →
демон снова поднят без ручного запуска.

---

## Этап 6 — reconfig и настройки

`sayclip reconfig` — интерактивная перенастройка: путь к whisper/модели,
список доступных моделей (сканирование директории), язык распознавания,
базовый промт для Whisper.

**Тест:** `sayclip reconfig` → меняешь язык и модель → следующая
транскрибация уже использует новые настройки, без правки конфига руками.

---

## Этап 7 — README и релиз MVP (Linux)

`README.md` по образцу debash (О приложении / Системные требования /
Установка / Настройка / Использование / Зависимости / Лицензия), на
английском — приложение само говорит по-английски, README ему под стать
(в `.dev/` при этом остаётся русский, это только пользовательские
заметки). MVP сознательно ограничен Linux — Windows отложен, см. «Позже».

**Тест:** `npm pack --dry-run` — в тарболе есть `README.md`, `bin/`,
`src/`, `contrib/`; дальше релизный цикл как обычно (пользователь сам).

---

## Мелкие доработки (пост-релиз)

Найдены при реальном тестировании на второй машине (после Этапа 7,
v0.1.3 → v0.1.4), не отдельные этапы плана.

* [x] Выходной файл whisper (`-otxt`) на другой сборке whisper.cpp
  писался не туда, куда мы ожидали читать — `ENOENT` при чтении
  `<wav>.txt`. Починено: `transcribe.js` теперь явно задаёт `-of
  <wavPath>`, не полагаясь на поведение бинарника по умолчанию.
* [x] Очень короткое (случайное) зажатие хоткея → whisper не успевает
  дать вообще ничего → тот же `ENOENT`, пугающая ошибка на ровном
  месте. Починено: `listen.js` теперь пропускает транскрибацию, если
  хоткей был зажат меньше `MIN_RECORDING_MS` (400мс), тихо печатает
  `(too short, skipped)`.
* Отдельно всплыла классическая галлюцинация Whisper на тишине —
  «Редактор субтитров Н.Закомолдина Корректор В.Сухиашвили» (Whisper
  так реагирует на пустое аудио на русском). Причина оказалась не в
  коде: микрофон физически остался на другой машине при переключении
  KVM-свитчем (клавиатура/мышь/монитор переключились, микрофон — нет).

---

## Этап 8 — Windows-порт + трей-иконка статуса

Написано за один заход без доступа к живой Windows-машине — код готов,
но **не проверен на Windows**, только там, где можно было проверить
независимо от платформы (Linux).

* Запись — `record.js`: `arecord` (Linux, как раньше) / `ffmpeg -f dshow`
  (Windows). Имя микрофона на Windows не автодетектится (парсинг вывода
  `ffmpeg -list_devices` слишком хрупкий, чтобы городить его вслепую) —
  спрашивается один раз (`recordingDevice` в конфиге, как и путь к
  whisper), команда для поиска имени печатается прямо в промпте. Стоп
  записи на Windows — не SIGINT (его там нет), а `q` в stdin ffmpeg.
* Буфер обмена — `clipboard.js`: добавлена ветка `clip.exe` (Linux-ветка
  `xclip`/`xsel`/`wl-copy` не тронута).
* Уведомления — `notify.js`: на Windows `node-notifier` (toast), Linux
  как был — прямой `notify-send`.
* **Трей-иконка** (новое, не было в исходном плане — попросили при
  обсуждении этапа): `tray.js` на `systray2`, кроссплатформенно
  (Windows и Linux сразу, без разделения). Жёлтый — идёт
  запись/распознавание, зелёный — готов/предыдущий результат уже в
  буфере. Иконки — сплошной PNG/ICO, генерируются на лету (`icon.js`),
  без внешних файлов.
* Два реальных бага апстрима, вскрытых и обойдённых по пути в
  `systray2`: (1) `fs.chmod(bin, '+x')` — шелловый синтаксис вместо
  числового режима, Node молча падает, бинарник трея остаётся без
  прав на выполнение — обходим, сами делаем `chmod 755` перед стартом;
  (2) двойной default-export при ESM/CJS-интеропе (`m.default.default`
  вместо `m.default`) — разворачиваем вручную.

**Проверено мной (не требует Windows):** трей стартует, переключает
жёлтый/зелёный, завершается без осиротевших процессов (Linux); генерация
PNG/ICO подтверждена независимо через PIL и `file`; `clipboard.js`/
`notify.js`/`transcribe.js`/`reconfig` — без регрессий на Linux-ветке
после рефакторинга.

**Не проверено, нужна реальная Windows-машина:** весь путь записи через
`ffmpeg`/dshow целиком, `clip.exe`, Windows-уведомления через
`node-notifier`, реально ли выглядит трей-иконка (а не только процесс
жив).

**Тест:** тот же сценарий, что на этапе 4, но на чистой Windows-машине —
push-to-talk → буфер обмена → уведомление → жёлтый/зелёный статус в
трее.

---

## Этап 9 — Автоустановка whisper (без ручной сборки)

Проблема: сборка/установка whisper.cpp вручную — самый большой барьер входа
для нетехнического пользователя, перечёркивающий весь смысл "просто
поставь и говори".

* Runtime whisper.cpp запакован вручную по одному разу под AVX2 на каждую
  платформу — минимальный набор файлов (whisper-cli/.exe + 4 общих
  библиотеки, без server/bench/quantize/parakeet/тестов — точный список
  вычислен через `ldd`, не угадан): `whisper-runtime-linux-avx2.tar.gz`
  (~1.4 MB), `whisper-runtime-win-avx2.zip` (~1.1 MB). SHA-256 на оба
  зашиты в `src/runtime.js`. Хранятся как GitHub Release assets в самом
  репозитории sayclip (не в npm-пакете — раздувать тарбол незачем), тег
  `whisper-runtime-v1`.
* Найден и исправлен реальный баг до релиза, не гипотетический: собранный
  `whisper-cli` несёт зашитый абсолютный RUNPATH на директорию сборки (мою
  локальную) — на чужой машине без явного `LD_LIBRARY_PATH` бинарник
  вообще не нашёл бы свои `.so`. Подтверждено через `strace`:
  `LD_LIBRARY_PATH` перебивает RUNPATH — `transcribe.js` теперь всегда
  выставляет его в директорию бинарника на Linux.
* `src/runtime.js` — `ensureWhisperRuntime()` (скачать → проверить
  SHA-256 → распаковать через системный `tar`; на Windows `tar.exe` это
  bsdtar, понимает и `.zip` тем же вызовом) и `ensureWhisperModel(name)`
  (tiny/base/small/medium с открытого HuggingFace-зеркала
  ggerganov/whisper.cpp — без хеш-пиннинга, так же делает и официальный
  скрипт whisper.cpp, это не понижение доверия относительно апстрима).
* `src/config.js` — при первой настройке и в `reconfig` теперь выбор:
  скачать автоматически (по умолчанию) или указать свой путь — отдельно
  для бинарника и для модели. Список моделей в `reconfig` объединяет уже
  скачанные/локальные файлы (сканирование директории, как раньше) с ещё
  не скачанными вариантами.
* `sayclip start` теперь отказывается стартовать демон, если whisper не
  настроен вообще — раньше в этом случае фоновый процесс завис бы,
  ожидая ответ на вопрос, который некому задать (нет терминала).

**Проверено (не требует Windows или ещё не созданного релиза):**
* Упакованные Linux-бинарники реально работают в изоляции — распакованы
  в чистую директорию, прогнаны через настоящий whisper-cli на
  синтетическом wav, `AVX2 = 1` в логе, exit 0.
* `ensureWhisperModel()` — настоящая скачка tiny-модели (77.7 МБ) с
  HuggingFace в изолированный HOME; прогресс троттлится, не флудит
  терминал.
* Ветка "свой путь" на первом запуске (и для бинарника, и для модели) —
  пишет корректный `config.json`.
* `reconfig` со всеми Enter — ничего не меняет, сетевых вызовов не
  делает.
* Guard на `sayclip start` — с пустым конфигом отказывает с понятным
  сообщением и кодом выхода 1 и не спавнит демона; с реальным конфигом
  стартует как раньше.
* Полный regression: `transcribe` на реальном конфиге, `npm pack
  --dry-run` (11.6 kB пакет, 15 файлов) — без изменений в поведении.

**Не проверено, нужно от тебя:**
1. Архивы лежат в scratchpad-директории этой сессии (`whisper-runtime/`:
   оба архива + `checksums.txt`) — нужно залить их как GitHub Release
   assets:
   ```
   gh release create whisper-runtime-v1 \
     whisper-runtime-linux-avx2.tar.gz \
     whisper-runtime-win-avx2.zip \
     --title "whisper.cpp AVX2 runtime v1" \
     --notes "Minimal whisper.cpp AVX2 build for sayclip's auto-install. Linux: whisper-cli + libwhisper/libggml*.so (ldd-verified minimal set). Windows: whisper-cli.exe + matching DLLs. SHA-256 in checksums.txt."
   ```
   (тег и оба имени файлов должны совпасть с `RUNTIME_ASSETS` в
   `src/runtime.js` — я их туда уже захардкодил).
2. Само скачивание нашего рантайма ("вариант 1" в меню) можно проверить
   только после этого релиза — до сих пор не запускался ни разу целиком.
3. На Windows: `tar.exe` для распаковки `.zip` встроен с Windows 10
   1803+ — на более старой системе установка автоматически не сработает
   (останется путь "свой путь").

**Тест:** после `gh release` — чистая машина (или чистый `~/.sayclip`),
`sayclip` без конфига → выбрать "скачать автоматически" для бинарника и
модели → работает push-to-talk без единого ручного шага установки
whisper.

---

## Мелкие доработки (пост-Этап-9, по итогам полного теста на Windows)

* [x] `sayclip stop`/Ctrl+C оставляли трей-иконку висеть до наведения
  мышью — обработчик `SIGTERM`/`SIGINT` убивал процесс, не дождавшись,
  пока `systray2` попросит трей-хелпер корректно убрать иконку. Починено:
  `stopTray()` в `tray.js` дожидается штатного `systray.kill()` (с
  таймаутом-страховкой 1с на случай, если хелпер уже мёртв), `listen.js`
  теперь зовёт его перед выходом. Заодно убрана гонка в самой кнопке
  "Quit" трея (она делала `kill()` и сразу `process.exit()`, не дожидаясь
  того же самого).
* [x] На Windows при каждой записи/распознавании мигало консольное окно
  (отдельно для ffmpeg, отдельно для whisper-cli) — не в foreground-режиме
  (`sayclip`), только через `sayclip start`. Причина: процессы без своей
  консоли (демон в фоне) при спавне дочерних Windows-процессов открывают
  им новую видимую консоль по умолчанию. Починено: `windowsHide: true` у
  обоих спавнов (`record.js`, `transcribe.js`; на Linux не действует).
* [x] `reconfig` — список моделей включал служебные тестовые файлы
  whisper.cpp (`for-tests-ggml-*.bin`, `for-tests-*` вообще, silero/
  parakeet и т.п.). Починено: фильтр теперь белым списком по маске
  `^ggml-.*\.bin$`, а не просто "любой .bin".
* [x] Уведомление после транскрибации нельзя было отключить (на Windows
  особенно громкое). Добавлен вопрос в `reconfig` (`notifications`,
  по умолчанию включено); при выключении подавляются обычные
  уведомления, но не критические (ошибка транскрибации всё равно
  покажется).
* [x] Добавлена `sayclip restart` — посылает `SIGTERM` текущему демону,
  ждёт (до 3с) его реального завершения, затем стартует заново. Не
  полагается на то, что старый процесс исчез мгновенно.
* Короткая пауза перед началом фактической записи первого слова —
  проверено: в коде нет искусственной задержки перед стартом записи
  (`MIN_RECORDING_MS` проверяется только на отпускании клавиши, не
  тормозит старт). Похоже на задержку инициализации самого
  ffmpeg/arecord при открытии устройства — не баг sayclip.
* [x] **Иконка трея** — заменена со сплошных цветных квадратов на
  настоящий рисунок микрофона: пользователь сам нарисовал белую
  (`tray-ready.png`) и жёлтую (`tray-recording.png`) версии 32×32,
  лежат в `src/assets/` (шлются с npm-пакетом). `icon.js` больше не
  генерирует PNG сам — только оборачивает готовый PNG в `.ico`-контейнер
  для Windows (размер читается из самого PNG, не хардкодится). Исходный
  комбинированный спрайт (обе версии сшиты по вертикали для просмотра
  рядом) сохранён в `.dev/assets/` — не шлётся с пакетом.
* [x] **Автозапуск при загрузке системы** — вопрос в `reconfig`
  (`add`/`remove`/Enter — оставить как есть), не прерывает остальной
  reconfig при неудаче (try/catch вокруг попытки, ошибка печатается и
  флоу идёт дальше). `src/autostart.js`: Linux — systemd `--user` юнит,
  генерируется на лету с абсолютными путями к node и `bin/sayclip.js`
  (без символических ссылок и `sudo`, в отличие от старой ручной
  инструкции в `contrib/` — obходит nvm/PATH проблему по-другому,
  надёжнее). Windows — запись в `HKCU\...\Run` через `reg.exe`, без
  сборки `.lnk`-файлов. Протестировано пользователем локально
  (`add` → `Autostart enabled.` → `systemctl --user status` показал
  включённый юнит), полный ребут системы ещё не проверялся.

---

## Позже (за пределами MVP)

* macOS-порт;
* автодетект микрофона на Windows (сейчас — вручную через конфиг);
* рантайм-детект generic/AVX для CPU без AVX2 (сейчас — только AVX2,
  без определения на старте);
* несколько профилей языка/модели с быстрым переключением без reconfig;
* альтернативные STT-бэкенды (не только whisper);
* возможность перепривязать хоткей (сейчас захардкожен Right Ctrl).
